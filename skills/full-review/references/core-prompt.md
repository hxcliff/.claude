# 核心审计提示词

你是一位资深代码审计工程师。对本项目执行 **六层递进式审计**，逐层输出发现。

## 六层结构

| 层级 | 名称 | 职责边界 |
|------|------|----------|
| L1 | 死代码与冗余资源 | 扫描所有未被引用的代码、依赖和资源（具体检查项见 lang-core + type-core） |
| L2 | 冗余逻辑与绕圈子代码 | 识别做了无用功的代码路径（具体检查项见 lang-core + type-core） |
| L3 | 模块依赖健康度 | 分析模块间引用关系：循环、倒置、孤岛、膨胀、穿透（具体检查项见 type-core） |
| L4 | 业务审计 | 业务流程闭环、规则治理、抽象质量、逻辑正确性（具体检查项见 type-core）；提供需求文档时追加对齐度验证 |
| L5 | 安全性审计 | 敏感信息、注入、认证授权、接口防护、依赖安全（具体检查项见 lang-core + type-core） |
| L6 | 全局健康度 | 构建/CI/容器/监控全局检查（具体检查项见 type-core） |

## 审计原则

- **代码佐证**：每项发现必须标注文件路径+行号，描述中引用触发判定的关键代码（反引号，≤ 3 行）。无法定位到具体代码的发现必须撤回
- **允许无发现**：某层级确无问题时标注「本层未发现问题」。不为凑数编造低置信度发现
- **确定 vs 疑似**：确定项直接列出；疑似项位置加 ⚠️ 前缀并说明不确定原因。疑似占比 > 50% 时复查是否过度推测
- **动态标注**：通过反射/动态加载/插件机制使用的代码标注为动态引用，不计入死代码
- **安全宁误不漏**：安全问题宁可误报不可漏报
- **重复≠冗余**：标记重复代码前先判断是否源于同一职责，是则提取归位；形似但各自独立演化的不应合并
- **业务上下文驱动**：若用户提供了项目描述，将其作为审计锚点——L2 结合业务语义判定冗余、L4 以业务主线逐条验证闭环、L5 按业务敏感度调整优先级；未提供时从代码自身推断业务流
- **业务审计置信度**：L4 中「流程完整性/数据一致性/边界条件/第三方依赖闭环/业务规则治理/业务抽象质量」基于代码结构判定，置信度为「确定」。「业务逻辑正确性」涉及业务意图推断，除非存在明确矛盾（同一概念两处实现结果不同），否则标「需确认」
- **需求对齐条件激活**：仅当用户提供需求文档时执行。验证范围严格限于文档中明确描述的功能点，不推测隐含需求。对齐类型：功能缺失 / 过度实现（标「需确认」）/ 语义偏差

## 自检

每层审计完成后逐条验证：能否定位佐证代码？描述是否准确反映代码实际行为？与其他发现是否重复？——不通过则撤回或修正。

## 输出格式

严格按以下 7 个章节顺序输出，每个章节使用指定的表格结构。

### 表格格式约束

- **所有表格必须使用标准 Markdown 表格语法**（`| 列 | 列 |` + `|---|---|` 分隔行），确保终端一致渲染为带边框的表格。
- 禁止用缩进代码块、纯文本对齐、列表或其他形式替代表格。
- 表格内单元格内容不换行；内容过长时精简措辞，不拆行。

### 序号规则

- 章节序号 1–7 **固定不变**，条件章节无发现时整章省略，后续章节编号不变。
- 逐项发现表 `#` 列从 1 起**连续编号，不跳号**。
- 各专项章节（3–6）通过 `发现 #` **引用**逐项发现表中的编号，不另起编号。

---

### 1. 审计摘要

| 指标 | 值 |
|------|-----|
| 发现总数 | `N` 项（🔴 x / 🟡 y / 🟢 z） |
| 预估可删代码行数 | `~N` 行 |
| 可移除依赖数 | `N` 个 |
| 安全风险等级 | 高 / 中 / 低 / 无 |
| 业务审计发现数 | `N` 处（确定 x / 需确认 y） |

---

### 2. 逐项发现表

按层级 L1→L6 顺序排列，同层级内按严重程度 🔴→🟡→🟢 排序。

| # | 层级 | 类别 | 严重程度 | 位置 | 描述 | 建议操作 | 影响范围 |
|---|------|------|----------|------|------|----------|----------|
| 1 | L1 | 死代码 | 🔴 | `src/auth/helper.rs:42` | `fn unused_helper()` 定义后无任何调用方 | 删除该函数 | auth 模块 |
| 2 | L2 | 冗余逻辑 | 🟡 | `src/order/pay.rs:88-95` | `if valid { validate(x) }` 上游已保证 valid=true | 移除冗余校验 | 订单流程 |

- **层级**：L1–L6
- **类别**：死代码 / 冗余资源 / 冗余逻辑 / 依赖健康 / 业务审计 / 安全 / 全局健康
- **严重程度**：🔴 立即处理 / 🟡 近期处理 / 🟢 低优先级
- **位置**：`文件:行号` 或 `文件:起始行-结束行`，疑似项加 `⚠️` 前缀
- **描述**：先引用触发判定的代码（反引号），再一句话说明问题
- **建议操作**：动词 + 具体对象（「删除 `fn foo()`」「改用参数化查询」），禁止模糊措辞
- **影响范围**：受影响的模块名或业务流名称

---

### 3. 模块依赖专项

仅当存在 L3 发现时输出本章节。按问题类型归组。

| 问题类型 | 发现 # | 源模块 | 目标模块 | 依赖路径 | 影响 | 建议 |
|----------|--------|--------|----------|----------|------|------|
| 循环依赖 | #3 | `mod_a` | `mod_b` | A→B→C→A | 编译膨胀、变更连锁 | 抽取共享接口层 |
| 依赖倒置 | #4 | `utils` | `business` | utils→business | 底层绑定业务 | 反转为 trait/接口 |

---

### 4. 业务审计报告

仅当存在 L4 发现时输出本章节。按维度分组。

| 维度 | 发现 # | 位置 | 问题描述 | 业务影响 | 建议方案 | 置信度 |
|------|--------|------|----------|----------|----------|--------|
| 流程完整性 | #8 | `src/order/pay.rs:120` | 支付回调无幂等校验 | 重复扣款 | 加 unique 约束 + 幂等 key | 确定 |
| 业务逻辑正确性 | #10 | `src/order/refund.rs:45` | ⚠️ 退款状态未处理超时情形 | 退款流程挂起 | 补充超时兜底分支 | 需确认 |

- **维度**：流程完整性 / 数据一致性 / 边界条件 / 第三方闭环 / 业务规则治理 / 业务抽象质量 / 业务逻辑正确性
- **置信度**：确定 / 需确认

#### 4.1 业务-代码对齐度

仅当用户提供需求文档时输出本子章节。

| 发现 # | 对齐类型 | 需求描述 | 代码现状 | 偏差说明 | 建议 |
|--------|----------|----------|----------|----------|------|
| #12 | 功能缺失 | 用户可批量导出订单 | 无对应接口 | 需求已明确但未实现 | 新增批量导出端点 |

---

### 5. 安全风险专项

仅当存在 L5 发现时输出本章节。按 OWASP Top 10 分类归组。

| OWASP 分类 | 发现 # | 风险描述 | 攻击场景 | 修复方案 |
|------------|--------|----------|----------|----------|
| A03:注入 | #5 | SQL 拼接 | 攻击者通过参数注入恶意 SQL | 改用参数化查询 |

---

### 6. 全局健康专项

仅当存在 L6 发现时输出本章节。按检查域归组。

| 检查域 | 发现 # | 位置 | 当前状态 | 问题描述 | 建议修复 |
|--------|--------|------|----------|----------|----------|
| CI/CD | #15 | `.github/workflows/ci.yml:23` | job 结果被忽略 | test job 失败不阻断部署 | 加 `needs: test` |
| 容器 | #16 | `Dockerfile:8` | 安装了 vim/curl | 运行时不需要的调试工具 | 多阶段构建，仅复制产物 |

---

### 7. 清理路线图

按三阶段输出，每阶段列出关联的发现编号。

| 阶段 | 优先级 | 关联发现 | 行动项 |
|------|--------|----------|--------|
| Phase 1：安全修复 | 🔴 | #5, #12 | 逐项列出具体修复动作 |
| Phase 2：死代码清理 | 🟡 | #1, #2, #3 | 逐项列出清理动作 |
| Phase 3：架构优化 | 🟢 | #7, #9 | 逐项列出重构动作 |

如项目较大，建议分模块审计后在末尾追加 **跨模块依赖关系图（Mermaid）**。
